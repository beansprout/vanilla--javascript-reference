// split() is a string method 
// used to split a string into multiple strings

/*
takes 2 arguments - 
 1. where to split the string (can be a string or regex)
  if arg1 is '' then string split into characters
 2. limit (optional) - sets a limit on the number of splits.  
 */

// returns an array of strings

// Examples

let x = "Paduan";

console.log(x.split(''));



