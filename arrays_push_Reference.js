const numbers = [];
// DOES THIS: adds one or more elements to the end of an array
numbers.push(15);
console.log(numbers); // returns [15]

// now numbers = [15]
// lets push something else and see what the function returns...

// push method RETURNS the new length of the array
console.log(numbers.push(23)); // returns 2


